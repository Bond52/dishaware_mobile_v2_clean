/// Feature flags pour activer/désactiver des blocs UI sans supprimer le code.
class Features {
  /// Section "Cette préparation vous plaît ?" + "Préparations testées".
  static const bool enablePreparationFeedback = false;

  /// Section tags dynamiques (chips 👍/👎).
  static const bool enableDishTagLearning = true;
}
